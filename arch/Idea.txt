생각을 정리해본다.

내가 만들어야 하는 것.
woody_woodpacker 이름의 packer
출력물 woody

woody_woodpacker 가 할 일
- 암호화
- 파일의 유효성 검사
- 32/64 비트 정규화
- 랜덤 키 생성
- 파라미터로 key 받기
- woody 파일 생성
- assembly 의 매직넘버 찾아서 변경

assembly 코드 가 할일
- 복호화 진행
	- 복호화 할 때 필요한 것들 (물리 elf 건드리지 않음)
		1. _start 와 상대적 거리
		2. 복호화 키
		3. base 와 복호화 함수의 offset
		4. base의 크기 , 메타데이터 크기
- woody 출력
....WOODY....
assembly 가 필요한 값들

생각 정리
- elf 파일을 살펴보니 패턴을 찾았다. 
일반적인 경우는 PT_LOAD 가 독립적으로 존재하고, 그안에서 여러개의 영역으로 쪼개진다.
또한 OS_Loader 가 프로세스를 올릴때 꼭 필요한 값인 p_offset:  0x0 이고 p_flags: 0x4 Read 인 PT_LOAD 는 암호화 하면안된다.
woody_woodpacker 를 해결 하기위해서는 나는 meta data 를 사용할 것이다.
매직넘버는 사용 하지 않고 그만큼의 공간이 있다고 가정한다.
meta data 가 가지고 data 의 종류
복호화 시작해야하는 offset
복화 해야하는 크기
flag 의 값

어셈블리 코드 동작 
추상화 매크로 사용
meta data 기준으로 복호화 진행
flag 값에 따라서 mprotect(2) 를 진행
복호화 진행
반복
끝났다면 _start로 이동

가상주소 - 현재주소 = 
call / pop 에서 call function 은 몇 바이트 일까
1fb:   e8 40 00 00 00          call   0x240
5바이트인걸 알 수 있다. 그렇다면 base 를 call/pop 전략에서 -5 byte 를 call 한 함수의 주소 기준으로 구할 수 있다.
nasm -f bin -o shellcode.bin testPackerToDecoding.s
ndisasm -b 64 shellcode.bin
objdump -D -b binary -m i386:x86-64 -M intel shellcode.bin

---
stub 복호화 코드 만들기


```

_stub_end:
	POP_ALL_REGISTER
	FIND_DATA_INDEX CTX_DI, 123, 2, 0
	FIND_OFFSET CTX_AX, [CTX_DI]
	jmp CTX_AX


```

마지막에 레지스터를 건드리면 안되는데 레지스터를 건드려서 jmp 를 하면 문제가 생길 수 있다.
ret 은 특수 레지스터를 건들여서 문제를 해결 한다. 이걸 활용해보자 
_start 문서를 확인 해보니까 건드리는 레지스터는 rbp, rsp, rdx 이다. 그럼 저 로직을 사용해도 된다.
근데 더 최적화를 해보자면
```
_stub:
	INIT_RESI CTX_CX, CTX_CX
	FIND_DATA_INDEX CTX_CX, 123, 2, 8
	INIT_RESI CTX_AX, CTX_AX
	FIND_OFFSET CTX_AX, [CTX_CX]
	push CTX_AX
	PUSH_ALL_REGISTER
	INIT_RESI CTX_AX, CTX_AX
	FIND_DATA_INDEX CTX_AX, 123, 5, 8 ; get meta data num
	mov %2, (%1 * %3)


_stub_end:
	POP_ALL_REGISTER
	ret

```

ret 은 rsp 상단에 있는 주소로 점프를 한다.
_start 는 rax, rcx 레지스터의 보존을 원하지 않는다. 그래서 미리 값을 계산해서
stack 의 맨처음에 넣어두고, 맨 마지막에 ret 을 헤버린다.

2026-02-11
stub.s  meta data 안의 p_falg 를 그냥 반전 시켜서 줘버리자

```
➜ xxd -i shellcode_64.bin | tail -n 1
unsigned int shellcode_64_bin_len = 496; 0x1F0
➜ xxd -i shellcode_32.bin | tail -n 1
unsigned int shellcode_32_bin_len = 352; 0x160
```

key 가 출력이 안됨
- ft_printf -> printf 로 교체

woody 에서 segment error 발생
- .dynamic 을 건드려서 문제가 발생. 
- check_meta 의 fill_arrys 안의 조건에 (phdr->p_flags & (PF_R | PF_X)) == (PF_R | PF_X) 추가

함수가 정상동작 안함
sample 은 HelloWrold 를 출력해야하지만 아무 것도 안함
현재 문제를 분석 결과 
```
gef➤  p/x $rax
$10 = 0x1000
gef➤  p/x $r10 -> ORI_BASE
$11 = 0x555555555175
```
```
gef➤  vmmap
[ Legend:  Code | Stack | Heap ]
Start              End                Offset             Perm Path
0x0000555555554000 0x0000555555555000 0x0000000000000000 r-- /home/jinseo/jinseo/42_gs/packer/woody
```
ori_base 가 0x0000555555554000 이 나와야 하지만 현재 0x1175 의 차이가 발생했다. 즉 ori_base 가 이상하기 때문에 모든 연산이 뒤틀린 것으로 추정 할 수 있다.
왜 값이 달라진건지 추정 해야한다. 
일단 내가 알고 있는건 ori_base 는 call/pop 을 쓰는데 왜 그런건지 한번 다시 찾아보자
현재 문제는 저 ori_base 가 _stub 의 주소를 구한다는 사실이다. 
설명을 하자면 철수는 10 칸 앞으로 갔다. 그리고 한칸 앞으로 간다음 지도를 보고 좌쵸 계를 얻었다. 그리고 자기가 한칸 욺직인 1 을 뺐다.
즉 현재 자기가 어떤 좌표계에 있는지 알게 된 것이다.
현재 ; dq(8byte) Entry point address:  -> index 3 으로 변경했으며 
ori_base 에서 빼주는데 어떻게 될지 모르겠다.
현재 복호화 는되는 것같은데 문제는 두번째 mprotect 함수를 사용할때 rdx 즉 세번째 인자가 이상하게 들어가서 오류가 난다. 
prot 값이 들어가야하는데 rdx 값이 rsi의 값이 들어가고 메모리 보호 flag 가 이상하게 돌아간다. gef > catch syscall mprotect  로 systemcall 을 추적할 수 있다.
문제는 USE_MPROTEC  에서 CTX_SI 를 초기화 하고 그 값을 다시 CTX_DX 에 넣어준다.  -> falg 값을 push 하고 다시 pop 해서 뽑기로 했다.

이제는 segmentaion 이 발생한다.


지금 문제가 return 하는 주소가 이상한 것 같다. 왜일까 한번 검사를 처음 stack 을 쌓는 곳을 봐보자 처음에 return 하는 주소도 이상하다. 아까 와 같다. 
위에 예시를 들었던 철수가 이상 행동하는 것 이랑 똑같은 상황이다. 
또 문제가 생긴 이유는 암호화 하는 부분과 복호화 하는 부분이 다르게 동작했기 때문이다.
내일 은 암호화 하는 부분을 더 보충 해보도록하자

2026-02-13
오늘은 더 암호화 를 해보기 위해서 fill_arrays_<arch_bit> 의 조건을 세밀하게 만들어보았다.
//((phdr->p_flags & (PF_R | PF_X)) == (PF_R | PF_X) || (phdr->p_flags & (PF_R)) == (PF_R))
이전에는 rw 만 검사를 했지만 현재는 r 도 검사를 하도록 바꾸었는데, 과연 동작을 할 지안할지 모르겠다.
동작을 안한다. segmentaion error 가 발생한다. 이유가 뭘까 확인 해보니 또 동적 loader 부분을 건드린 것 같다
```bash
gef➤  x/g $rip
0x7ffff7fe6215 <dl_main+6453>:  0x1e87f641085a8b48
gef➤  bt
#0  audit_list_add_dynamic_tag (tag=0x6ffffefc, main_map=0x7ffff7ffe2e0, list=0x7fffffffdf70) at ./elf/rtld.c:233
#1  dl_main (phdr=<optimized out>, phnum=<optimized out>, user_entry=<optimized out>, auxv=<optimized out>) at ./elf/rtld.c:1788
#2  0x00007ffff7fe283c in _dl_sysdep_start (start_argptr=start_argptr@entry=0x7fffffffe2e0, dl_main=dl_main@entry=0x7ffff7fe48e0 <dl_main>) at ../elf/dl-sysdep.c:256
#3  0x00007ffff7fe4598 in _dl_start_final (arg=0x7fffffffe2e0) at ./elf/rtld.c:507
#4  _dl_start (arg=0x7fffffffe2e0) at ./elf/rtld.c:596
#5  0x00007ffff7fe3298 in _start () from /lib64/ld-linux-x86-64.so.2
#6  0x0000000000000001 in ?? ()
#7  0x00007fffffffe5a4 in ?? ()
#8  0x0000000000000000 in ?? ()

```
로그를 보니 맞다. 지금 dynamic 부분을 건드리고 있다.
문제는 비트 마스크 부분인 것 같은데
((phdr->p_flags & (PF_R | PF_X)) == (PF_R | PF_X) || (phdr->p_flags & (PF_R)) == (PF_R))
이 부분이 문제인 것같다.
r 이 하나라도 들어 있다면 (phdr->p_flags & (PF_R)) == (PF_R) 이조건에 부합해서 문제가 생긴다.
r 만 들어 있는 상태를 확실하게 검사하기 위해서는 어떤 방법이 있을까
생각을 해보니까 (value & 7) == PF_R 을 확인하면된다. 이렇게 검사를 하면 그냥 r 하나면 PF_R 이랑 같아지겠지
그리고 or 연산은 왼쪽이 맞으면 오른쪽은 검사를 안한다. 즉 이미 어느정도 분기는 처리가 된다.
역시 저 문제였다. r 이 들어가 있으면 문조건 ok 를 한다. 이건 문제가 된다. 왜 내하면 dynamic loader 는 내가 아직 제어 하기 힘들기 때문제 저 부분은 건드리면 안된다.
```bash
➜  packer git:(main) ✗ nasm -f bin -o shellcode_32.bin stub.s
➜  packer git:(main) ✗ nasm -DARCH_64 -f bin -o shellcode_64.bin stub.s
➜  packer git:(main) ✗ xxd -i shellcode_64.bin
➜  packer git:(main) ✗ sdiff <(objdump -D woody) <(objdump -D sample)
➜  packer git:(main) ✗
```

32비트에서 연산이 다르게 될 경우를 대비
int		encrypt_segment_32(unsigned char *base, uint64_t limit,
			const t_encryption *enc, uint64_t key);
만들기 완료
