오늘 할 일

1. 아래 함수들의 main page 를 번역한다.(시간나면 영어로 읽어본다.) : clear
    ◦ fputs, fflush, lseek(2:64->9, 32->19)
    ◦ mprotect(2:64->10, 32->125 )
    ◦ syscall
    - AI 한테 시키고 나는 다른일을 한다.
    - 일단 systemcall 함수인지 찾아보자
2. elf 에서 프로그램 헤더를 추출하고 한번 값을 본다. :
    - ft_nm 에서 사용했던 포인터 산술연산 기법을 사용한다.
    - utils 에서 사용했던 코드를 사용한다.
3. c언어코드를 한번 elf 에 삽입해보는 상황을 만들어 본다.
    - 주의 사항 -> 우아하게 종료를 해야한다.

내가 해석 해야하는 정보

e_entry : 프로세스가 시작하는 가상 주소 제공 e_entry - base = offset 이다.
e_phoff : pgdr table offset byte 단위
e_phentsize : 프로그램 헤더 table에 있는 하나의 entry
e_phnum : 프로그램 header 의 table 의 수

- p_type : 이 멤버는 이 배열 엔트리가 기술하는 세그먼트의 유형을 나타내거나 배열 정보를 해석하는 방법을 나타냅니다. 유형 값과 그 의미는 아래에 설명되어 있습니다.

- p_offset : 이 멤버는 파일의 시작부터 세그먼트의 첫 번째 바이트가 위치하는 곳까지의 오프셋을 보유합니다.

- p_vaddr : 이 멤버는 세그먼트의 첫 번째 바이트가 메모리에 위치하는 가상 주소를 보유합니다.

- p_paddr : 물리 주소 지정이 적절한 시스템에서, 이 멤버는 세그먼트의 물리 주소를 위해 예약되어 있습니다. 시스템은 실행 프로그램에 대해 물리 주소 지정을 무시하므로, 이 멤버는 현재 정의되지 않은 내용을 가집니다.

- p_filesz : 이 멤버는 세그먼트의 파일 이미지 바이트 수를 보유하며, 0일 수 있습니다.

- p_memsz : 이 멤버는 세그먼트의 메모리 이미지 바이트 수를 보유하며, 0일 수 있습니다.

- p_flags : 이 멤버는 세그먼트와 관련된 플래그를 보유합니다. 값과 의미는 아래에 설명되어 있습니다.

- p_align : 이 멤버는 세그먼트가 메모리와 파일 내에서 정렬되는 값을 보유합니다. 값 0과 1은 정렬이 필요 없음을 의미합니다. 그렇지 않으면 `p_align`은 2의 양수 거듭제곱이어야 하며, `p_vaddr`은 `p_align`을 모듈로(modulo)로 하여 `p_offset`과 동일해야 합니다.


- PT_NULL : 배열 엔트리가 사용되지 않음을 나타냅니다. 다른 멤버의 값은 정의되지 않습니다. 이 유형을 사용하면 프로그램 헤더 테이블에 사용되지 않는 엔트리를 가질 수 있습니다.

- PT_LOAD :`p_filesz` 및 `p_memsz`로 기술되는 로드 가능한 세그먼트를 지정합니다. 파일의 바이트는 메모리 세그먼트의 시작 부분에 매핑됩니다. 세그먼트의 메모리 크기(`p_memsz`)가 파일 크기(`p_filesz`)보다 크면, "남은" 바이트는 값 0을 가지도록 정의되며 세그먼트의 초기화된 데이터 영역 뒤에 옵니다. 파일 크기는 메모리 크기보다 클 수 없습니다. 프로그램 헤더 테이블의 로드 가능한 세그먼트 엔트리는 `p_vaddr` 멤버를 기준으로 오름차순으로 정렬되어 나타납니다.
- PT_DYNAMIC : 배열 엔트리가 동적 링크 정보를 지정합니다.

- PT_INTERP : 배열 엔트리가 인터프리터(interpreter)로 호출될 널 종료 경로명의 위치와 크기를 지정합니다. 이 유형은 실행 파일에만 의미가 있으며(공유 객체에는 나타날 수 있음), 공유 객체에는 나타나지 않을 수 있습니다. 두 번 이상 나타날 수 없습니다. 만약 존재한다면, 모든 로드 가능한 세그먼트 엔트리보다 앞서야 합니다.

- PT_NOTE : 배열 엔트리가 보조 정보의 위치와 크기를 지정합니다.

- PT_SHLIB : 이 세그먼트 유형은 예약되어 있지만 지정되지 않은 의미론을 가집니다. 이 유형의 엔트리를 포함하는 프로그램은 ABI를 준수하지 않습니다.

- PT_PHDR : 배열 엔트리가 파일 및 프로그램의 메모리 이미지 내에서 프로그램 헤더 테이블 자체의 위치와 크기를 지정합니다. 이 세그먼트 유형은 프로그램 헤더 테이블이 프로그램의 메모리 이미지의 일부인 경우에만 나타날 수 있습니다. 만약 존재한다면, 모든 로드 가능한 세그먼트 엔트리보다 앞서야 합니다.

- PT_LOPROC : ~ PT_HIPROC 이 범위의 값은 프로세서 특화 의미론을 위해 예약되어 있습니다.
